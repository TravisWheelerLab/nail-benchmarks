*.DS_Store
/target/
# downloaded data is not tracked, but long-seqs' inputs are small and checked
# in — nothing fetches them, so ignoring them would empty a fresh clone
/data/*
!/data/long-seqs/
/tools/
# generated benchmark instances are benchmark-<size>/; the trailing dash
# matters, or this also swallows the benchmarks/ source tree
**/benchmark-*/
**/figures*/
**/profmark/
**/results/
# a pipeline run keeps the stderr of whatever failed, next to wherever it was
# started from
**/stderr/
**/tmp/
**/tmp-*/
**/__pycache__/
